# Copy to .env.local for local development.
# Next.js requires the NEXT_PUBLIC_ prefix for any variable read in the
# browser — this mirrors API_BASE_URL from the root .env.example.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_STUDIO_API_KEY=change-me
