# scripts

One-off dev/ops scripts: DB migration runner, seed data loader, local env
setup, agent-boilerplate generator. Anything invoked via `make` targets
in the root `Makefile` that isn't infra (docker compose) lives here.
