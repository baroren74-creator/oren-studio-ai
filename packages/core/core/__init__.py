"""Shared domain code: schemas, event types, and the Agent Registry.

See docs/agents.md and docs/api.md at the repo root for the reference
documentation this package implements. This package is depended on by
apps/, agents/, services/, and providers/ — never the reverse
(docs/standards.md section 2).
"""
