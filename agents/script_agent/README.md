# script_agent

Writes Hook, Body, CTA, Caption, Title, Hashtags together (one
structured LLM call) — in Oren's style, read from `style_profile`
(`docs/database.md`) when available, `docs/vision.md`'s baseline style
guide otherwise. Writes in Hebrew.

**Implemented** — `docs/roadmap.md` Phase 3.2-3.4, `docs/agents.md`'s
Script Agent section for the full write-up.
