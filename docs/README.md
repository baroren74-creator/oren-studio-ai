# docs

Source of truth for *why*, not just *what*. Start with `vision.md` and
`prd.md`, then `architecture.md` and `open-source-landscape.md`, then
`roadmap.md`. `decisions.md`, `agents.md`, `api.md`, `database.md`, and
`standards.md` are living references — update them in the same PR as any
code change they describe (see `CONTRIBUTING.md`).
