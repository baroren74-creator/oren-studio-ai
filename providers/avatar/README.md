# providers/avatar

Deferred (Phase 4.11, non-blocking). Candidate stack when built: MuseTalk
(MIT, audio-driven lip-sync) + LivePortrait (MIT code, but must first
replace its non-commercially-licensed InsightFace `buffalo_l`
face-detection dependency). See `docs/open-source-landscape.md` section 5.
