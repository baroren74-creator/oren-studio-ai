# providers/search

Tavily (default, generous free tier) + self-hosted SearXNG (high-volume/
exploratory queries, zero marginal cost). Brave Search API intentionally
excluded — it dropped its free tier in Feb 2026. See
`docs/open-source-landscape.md` section 6.
